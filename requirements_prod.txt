# Compatibility alias for the standard local non-container runtime set.
#
# Prefer:
#   pip install -r requirements_runtime_local.txt
#
# This file is kept so existing setup scripts using `requirements_prod.txt`
# still install the correct runtime dependencies.
-r requirements_runtime_local.txt

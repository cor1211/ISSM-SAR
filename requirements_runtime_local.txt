# ISSM-SAR local runtime dependencies
#
# Use this file when running the repo on a normal machine, not via Docker.
# It extends the Docker STAC runtime set with packages that the container gets
# from its base image or that are needed by the default local workflow.
#
# Notes:
# - The Docker image already provides `torch` from the base PyTorch image.
# - The default `config/pipeline_config.yaml` uses `gee_trainlike_composite`,
#   so `earthengine-api` is included here for local parity.

-r requirements_runtime_stac.txt

# Needed explicitly on a plain machine.
torch==2.8.0

# Needed for the default GEE workflow outside Docker.
earthengine-api==1.6.15
